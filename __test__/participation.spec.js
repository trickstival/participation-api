const request = require('supertest')
const { createApp } = require('../src/server')

describe('testing participation endpoints', () => {
  it('should be able to insert participations', async () => {
    expect(true).toBeTruthy()
  })
})
